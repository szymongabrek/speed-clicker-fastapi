## Simple Speed Clicker Game with FastAPI Server

```
    git clone https://github.com/szymongabrek/speed-clicker-fastapi.git
    pip install -r requirements.txt
    cd app
    uvicorn server:app --reload
```
